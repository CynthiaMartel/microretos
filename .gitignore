*.log
.claude/

# Backups de BD de producción bajados localmente — contienen datos reales (PII), nunca versionar
/backups_produccion/

# Proyecto del frontoffice — repo git propio e independiente (frontoffice_dualab),
# solo vive anidado aquí en disco. Nunca debe entrar en este repo.
/FRONTOFFICE/
INSERT_*.sql
INSERT_*.sql.zip

# Informes generados (se regeneran, no se versionan) — TAREAS_YA_REALIZADAS_PARA_LOVABLE.md sí se versiona
INFORME_*.md
INFORME_*.pdf
RESUMEN_*.md
RESUMEN_*.pdf

# Glosario generado para uso interno — no se versiona
/Glosario_DuaLab.pdf

# Notas internas de tareas pendientes — no debe ser público
/TAREAS_PENDIENTES.txt

# Scripts sueltos de prueba manual en la raíz (Playwright ad-hoc, no son tests del repo)
/test_*.js

# Contiene SSH_HOST/SSH_USER/SSH_PORT/SSH_KEY reales del servidor — nunca versionar
/deploy.sh
.DS_Store
.env*
!.env.example
.phpactor.json
.phpunit.result.cache
/.fleet
/.idea
/.nova
/.phpunit.cache
/.vscode
/.zed
*.code-workspace
/auth.json
/node_modules
/public/build
/public/hot
/public/storage
/storage/*.key
/storage/pail
/vendor
Homestead.json
Homestead.yaml
Thumbs.db
desahucio_.png
